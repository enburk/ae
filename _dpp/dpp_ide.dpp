#include "sys.h"

struct Button
{
    int x;
    auto y = x*x;
}

IDE
{
    ff



}

void sys::window::on::size()
{
}

void sys::window::on::draw(int x, int y, int w, int h)
{
    for (auto& c : sys::window::image) c = 0xFFFF0000;
}
