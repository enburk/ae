using "data";
;;using "system";

;; type size ;; = math::vector(2)
;; {
;;     x, y : integer;
;; }


size : type = math::vector(2, integer);

colors
{
    RGBA : type =
    {
    }
}

image(color) : type =
{
    size : size;
}

