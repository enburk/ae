operator (x) >>> (f) = f(x)
operator (f) <<< (x) = f(x)

;; operator if (x) then (y) else (z) =
;; {
;;     if x
;;     then return y;
;;     else return z;
;; }

;; operator (f) if (x) else (g) = ;; python

;; operator if (x) then (y) elif (z) then (a) … else (b) =









