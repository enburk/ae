import: ::numerals;
import: ::ranges;

let: memory = ::memory;


;; meta

function: size.of (type: x) → natural;
function: type.of (type: x) → type:;

type: value.of(type: x)
{
    function: at (address) → x;
}

type: x extension:
{
    property: self → pointer(x);
    property: same → type.of(self.value);
}






