system
{

    out
    {
        operator <<< (byte[]) {external}
    }

    ;; istream { operator >>> (byte[]) }
    ;; ostream { operator <<< (byte[]) }
    ;; iostream : istream, ostream {}
    
    ;; in  : istream;
    ;; out : ostream;
    ;; err : ostream;
}
