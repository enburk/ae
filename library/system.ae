mutation: in → byte[];
mutation: out (byte[]);
mutation: err (byte[]);

;;mutation in  → byte[] = …
;;mutation out ← byte[] = …
;;mutation err ← byte[] = …

;;mutation in→ byte[] = …
;;mutation out byte[] = …
;;mutation err byte[] = …

;; function sin real → real = …
;; function arctan real → real = …
;; function arctan ratio → real = …

type: app (type: window)
{
    window window;
}