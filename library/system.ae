using "gui";
using "system.c++";

function out (data: byte[]) → void = {...}
function err (data: byte[]) → void = {...}

window  : gui::window;
screen  : gui::screen;
screens : gui::screen[];










