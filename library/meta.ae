using "data";
using data::array;
using data::text;

type size;
type address;

function size.of (x) → size = {...}
function type.of (x) → type = {...}
function name.of (x) → text = {...}

type value.of(x) = { function at (a: address) → x = {...} }



